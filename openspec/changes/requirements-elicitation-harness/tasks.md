## 1. Readiness contracts and deterministic state

- [x] 1.1 Implement the minimal versioned requirements/authorization state and validation interface in the existing harness module, verifying focused unit tests reject unresolved blocking user-owned decisions and invalid provenance without affecting direct routes.
- [x] 1.2 Guard adaptive planning for assessments that declare requirements readiness, verifying ready pre-authorized/approved states pass while approval-required or unresolved states stop before packets.

## 2. Interview-first routing integration

- [x] 2.1 Add the concise inspect/ownership/interview/readiness protocol to agent-facing routing and OpenSpec proposal guidance, verifying contract tests cover question suppression, delegation, OpenSpec synthesis, approval stop, and explicit pre-authorization.
- [x] 2.2 Add apply-time requirements re-entry and packet pause/resume guidance, verifying a material ambiguity fixture returns to OpenSpec update and cannot resume until readiness is restored.

## 3. Regression and retrospective evaluation

- [x] 3.1 Add deterministic fixtures/tests for all ten required direct, SDD, interview, authorization, re-entry, and fresh-session scenarios and verify `npm run test:harness` passes.
- [x] 3.2 Add the non-executable GitHub Pages retrospective fixture with repo-owned facts, targeted user-owned decisions, and expected OpenSpec shape, verifying no showcase deployment or publishing configuration is changed.

## 4. Integration and review

- [x] 4.1 Run affected validation and strict OpenSpec checks, recording exact results in the execution validation ledger.
- [x] 4.2 Run diff-scoped Standards and Spec review from the fixed `origin/main` baseline, remediate valid findings, re-run required validation, and create a PR without merging it.
- [x] 4.3 Move requirements/authorization semantics out of upstream-managed propose/apply/update skills and verify a disposable `openspec update --force` cannot silently remove the repo-owned integration.
