## 1. Harness contracts and fixtures

- [x] 1.1 Add the repo-owned execution-plan configuration, schemas/examples, and Wave 7/current-change assessments with outcome-level packets; verify schema and retrospective contract tests pass.

## 2. Planner and context lifecycle

- [x] 2.1 Implement the deterministic assess/plan/context/handoff interface with configurable S/M/L/XL bands, dependency validation, bounded context indexes, and honest proxy labeling; verify focused Node tests cover valid, invalid, and fresh-session continuation cases.

## 3. Change-aware validation and evidence

- [x] 3.1 Implement affected validation selection, expensive-run fingerprint/invalidation enforcement, review-state reuse, and dynamic Git/evidence collection; verify docs-only, Modal-only, shared native-dialog, manifest, and repeat-full-suite fixtures.

## 4. Telemetry and operator flow

- [x] 4.1 Implement compact typed telemetry recording/summaries and ready-packet/operator commands without fabricated token usage; verify command/read/output/suite/review/scope/remediation counts and unavailable-runtime behavior in tests.

## 5. Repository integration

- [x] 5.1 Add focused package scripts and concise progressive-disclosure pointers in agent, OpenSpec, skill-routing, validation, review, and component-gate documentation while leaving generated OpenSpec skills untouched; verify formatting, instruction checks, and existing routing expectations.

## 6. Evaluation and regression

- [x] 6.1 Run the Wave 7 retrospective end-to-end and record executable evidence that it is classified large, decomposed by semantic seams, supports multiple sessions, preserves both component gates, and routes the four required validation cases correctly.

## 7. Integration, review, and delivery

- [x] 7.1 Run focused then final repository validation, perform diff-scoped Standards and Spec review, remediate valid findings in one batch, re-review only the remediation/current diff, verify `shlz-design-source/` is unchanged, and create the unmerged PR.
