## 1. Baseline and source contract

- [ ] 1.1 Record the clean task baseline, supplied SVG hashes, raw Calendar authority, existing calendar/date-picker census, CI and review state; verify source-integrity checks prove no file under `shlz-design-source/` changed
- [ ] 1.2 Extract and classify Calendar Grid geometry, paint, temporal states, disclosure states, density states, unavailable-day treatment, and unsupported behavior as source facts, supplemental evidence, repository decisions, or assumptions; verify focused source-contract tests cover every claimed state

## 2. Public module implementation

- [ ] 2.1 Add framework-neutral Calendar Grid semantic markup and source-backed CSS for row/date headers, temporal columns, items, counts, unavailable cells, density states, sticky context, and contained two-axis overflow; verify structural, style, and source-fidelity tests pass
- [ ] 2.2 Implement and export the idempotent Calendar Grid disclosure controller with validated relationships, row/cell operations, bubbling typed events, and destruction; verify unit tests cover success, invalid markup, independence, event detail, repeat enhancement, and teardown
- [ ] 2.3 Add package exports, generated distribution artifacts, public documentation, and plain-HTML consumption for the Calendar Grid interface and consumer-owned boundaries; verify package build and consumer smoke tests pass

## 3. Executable consumers and audit inventory

- [ ] 3.1 Add source-matrix, interaction, density, empty, and responsive Showcase fixtures plus one Data Workspace consumer using stable audit IDs and application-owned data/actions; verify both applications build and the grid contains no application shell contract
- [ ] 3.2 Split Calendar Grid evidence from source-only Date Picker, update project inventory, and add a Calendar Grid component manifest classifying every executable fixture, live consumer, diagnostic, and legacy/native substitute; verify manifest-contract and repository occurrence guards pass

## 4. Browser, accessibility, and visual evidence

- [ ] 4.1 Add focused browser coverage for native table relationships, real row/cell disclosure, keyboard focus, event emission, rerender/re-enhancement, destruction, temporal/unavailable descriptions, and consumer actions; verify runtime and accessibility assertions pass without substituting static states
- [ ] 4.2 Add focused visual and computed-geometry coverage for source-backed states, sticky row/date context, two-axis overflow, dense/empty/long localized content, narrow viewport, text scaling, and representative bounded performance; inspect and approve focused snapshots

## 5. Completion, review, and delivery

- [ ] 5.1 Run the complete component gate, manual interaction walk, source integrity, OpenSpec, lint, build, package, full browser, accessibility, visual, and regression validation; record exact occurrences, evidence levels, supported limits, limitations, CI, review threads, and Calendar Grid status without changing Date Picker status
- [ ] 5.2 Run independent Standards and Spec review against the immutable baseline, remediate every scope-local finding, rerun affected checks, complete route-conformance and delivery guards, push the task branch, open an unmerged PR to `main`, and verify required GitHub checks are green

