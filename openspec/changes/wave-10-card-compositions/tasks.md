## 1. Baseline and source contract

- [x] 1.1 Record the clean Wave 10 baseline, current CI/review state, authoritative source hashes, and source-critical geometry/paint/image facts; verify the three raw SVGs remain unchanged and every claim has an explicit classification.

## 2. Manifest and absence census

- [x] 2.1 Add the Card compositions schema-v2 manifest with independent source claims, empty runtime occurrence ledgers, precise evidence applicability, findings, and limitations; verify the manifest contract tests pass.
- [x] 2.2 Add a focused repository-wide source/absence census for bounded Card, Reports card, and Cover composition signatures; verify current absence passes, unrelated terminology is classified, and a synthetic matching production surface fails.

## 3. Evidence and disposition

- [x] 3.1 Reconcile the project inventory and create the Wave 10 report with exact hashes, source facts, zero occurrence counts, evidence applicability, limitations, CI/review state, and an independent source-only disposition; verify `implementation_status` remains `source-only` and `VERIFIED` is claimed only if the full applicable gate passes.

## 4. Validation, review, and delivery

- [x] 4.1 Run source integrity, OpenSpec, formatting/lint/build/package, manifest/census, affected, and final aggregate validation; verify exact results are retained and no raw source or runtime/public implementation was added.
- [x] 4.2 Review the complete episode diff independently against repository standards, the Wave 10 spec, authoritative SVGs, and non-goals; remediate every scope-local finding, re-run affected checks, satisfy receipt-based conformance/delivery, and deliver an unmerged green PR targeting `main`.
