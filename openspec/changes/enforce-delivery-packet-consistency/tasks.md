## 1. Regression contract

- [x] 1.1 Add a minimized PR #40 split-brain fixture and coherent control at the public `delivery-check` seam; verify the focused command deterministically accepts the divergent fixture before production changes and rejects a behavioral decoy.

## 2. Delivery reconciliation

- [x] 2.1 Require planned delivery telemetry and reconcile mandatory packet state, embedded execution evidence, telemetry boundary, and canonical handoff bindings; verify the RED fixture fails with packet-specific diagnostics while the coherent control reaches ordinary delivery validation.
- [x] 2.2 Update operator guidance for the new planned-delivery evidence input and verify examples match CLI behavior.

## 3. Review and delivery

- [x] 3.1 Run only impact-selected validation, strict OpenSpec validation, and target-diff inspection; record exact selected and excluded targets.
- [ ] 3.2 Independently review Standards and Spec/failure-path behavior, remediate blocking findings, run delivery guards, post CodeRabbit dispositions including the timezone evidence, push the PR #40 head, and leave it unmerged.
