## 1. Baseline and regression seam

- [ ] 1.1 Record the clean implementation baseline and verify the exact `Documents.svg` authority remains unchanged
- [ ] 1.2 Add a failing browser/source regression that rejects the pill trigger and compressed state grid while requiring the 467×102 full-surface label composition

## 2. Source-fidelity implementation

- [ ] 2.1 Replace all classified File Upload markup with the full-surface associated label, canonical cloud-upload icon, and one instruction; verify structural and native-activation tests pass
- [ ] 2.2 Restore source-default geometry and centered paint with fluid narrow/text-scale behavior; verify computed-style and focused visual tests pass
- [ ] 2.3 Replace the generic Showcase matrix with dedicated readable source/state specimens and verify each occurrence remains independently classified

## 3. Contract and evidence reconciliation

- [ ] 3.1 Update File Upload documentation, audit manifest, source claims, occurrence census, and project inventory without changing controller ownership; verify focused structural tests pass
- [ ] 3.2 Regenerate only affected File Upload snapshots, inspect them against `Documents.svg`, and record source-default plus responsive/state dispositions

## 4. Completion and delivery

- [ ] 4.1 Run the complete File Upload component gate and affected/full repository validation, recording exact checks, counts, manual interaction walk, and limitations
- [ ] 4.2 Run independent Standards and Spec review, remediate every scope-local finding, complete conformance/delivery guards, and open an unmerged PR to `main` with green CI/review state
