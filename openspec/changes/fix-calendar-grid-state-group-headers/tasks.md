## 1. Source contract and RED seam

- [ ] 1.1 Record fresh Calendar.svg facts, derived patterns, repository decisions, exact Showcase spans, occurrence inventory, and unchanged source hash in the Calendar Grid contract/audit evidence.
- [ ] 1.2 Add the focused semantic/geometry browser regression and structural checks, then capture a failure on the immutable baseline specifically because temporal colgroup headers are absent and state words are embedded in date labels.

## 2. Framework-agnostic contract and Showcase

- [ ] 2.1 Implement the standalone Past/Today/Future colgroup row and separate date row in the Showcase and plain-HTML fixture, verifying spans 1/1/3, accessible names, and unavailable independence.
- [ ] 2.2 Update Calendar Grid CSS and public documentation for group surfaces, sticky offsets, Today body continuity, unavailable composition, responsive overflow, and optional consumer-owned period rows; verify focused tests turn GREEN.

## 3. Component completion evidence

- [ ] 3.1 Update and inspect component-focused desktop/narrow screenshots that visibly include the temporal group row and verify focused visual fidelity plus responsive/content stress.
- [ ] 3.2 Run runtime, accessibility, consumer, occurrence-classification, and audit-manifest checks; record exact counts and limitations only when the Calendar Grid completion gate passes.

## 4. Validation, review, and delivery

- [ ] 4.1 Run OpenSpec validation, targeted Calendar Grid checks, Impeccable detection, and full `npm run check` in one process; verify Date Picker status and every file under `shlz-design-source/` are unchanged.
- [ ] 4.2 Run independent read-only Standards and Spec reviews against the immutable episode baseline without build/generate in the shared worktree, resolve findings, and pass route conformance and delivery guards.
- [ ] 4.3 Push the task branch, open a PR to `main`, resolve all review threads, wait for green required CI, and verify the PR remains open and unmerged.
