## 1. Evidence contracts

- [ ] 1.1 Inventory the unresolved source records by evidence cohort and bind each proposed disposition to an exact source identity plus existing committed provenance or audit ownership.
- [ ] 1.2 Add focused coverage checks for the supported cohorts and for the fail-safe rule that insufficient evidence remains `unresolved`.

## 2. Source coverage classification

- [ ] 2.1 Classify normalized standalone glyph and file-type records using the committed icon normalization provenance, then run the focused checks.
- [ ] 2.2 Classify only the nested, legacy, evidence-only, or application-owned records whose disposition is proven by existing repository evidence; retain every ambiguous reusable component as `unresolved`.
- [ ] 2.3 Record diagnostic impact for every affected classification decision without weakening source diagnostic visibility.

## 3. Generation and reporting

- [ ] 3.1 Regenerate the source coverage matrix and update its documentation with exact before/after metrics and an actionable list of the remaining reusable candidates.
- [ ] 3.2 Verify deterministic regeneration, valid path and family references, exact record/variant denominators, preserved source hashes, and unchanged runtime package surfaces.

## 4. Validation and delivery

- [ ] 4.1 Run focused and proportionate full validation, including strict OpenSpec validation.
- [ ] 4.2 Complete independent Standards and Spec review and resolve or explicitly disposition every finding.
- [ ] 4.3 Run post-discovery route-conformance and delivery guards, push the task branch, and open an unmerged pull request.
