## 1. Reproduce and Contract the Incident

- [x] 1.1 Add red-capable public harness regressions for positive direct routes, exact GitHub Pages routing/ownership/mutation blocking, post-diff escalation, and branch/PR safety; verify the focused tests fail for the incident symptoms before implementation

## 2. Route and Execution Guards

- [x] 2.1 Implement the compact structured route-evidence validator and pre-mutation eligibility guard; verify typo/refactor direct cases pass and material/unknown cases re-route
- [x] 2.2 Implement change-aware discovered-surface conformance; verify deployment-sensitive semantic changes block direct completion while harmless workflow maintenance remains direct
- [x] 2.3 Implement git execution and delivery guards; verify default-branch mutation/direct push fail and task-branch-to-PR evidence passes

## 3. Workflow Integration

- [x] 3.1 Convert the GitHub Pages retrospective fixture into route-eligibility regression input without duplicating normative requirements; verify exact intent retains unresolved `release-policy` and `public-url`
- [x] 3.2 Update repo-owned routing, requirements, execution, and agent-facing instructions for mandatory preflight/conformance/PR flow; verify OpenSpec regeneration safety remains green

## 4. Validation and Delivery

- [x] 4.1 Run focused harness tests, strict OpenSpec validation, and affected validation once at the stabilized fingerprint; record results in execution state
- [ ] 4.2 Run diff-scoped Standards and Spec review, remediate valid findings in one batch, run targeted re-review and any required aggregate validation, then create a PR without merging
