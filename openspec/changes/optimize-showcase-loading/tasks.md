## 1. Baseline and safeguards

- [x] 1.1 Record the clean current-main production build graph, raw/gzip asset sizes, initial/deferred classification, source-reference requests, tool versions, and Git identity; verify the baseline report rejects a changed commit or emitted asset hash.
- [x] 1.2 Add focused RED tests for complete hash/id ownership, initial-request exclusions, build-budget comparison, concurrent section requests, and failure/retry behavior; verify every new assertion fails for the expected pre-change reason before runtime implementation.

## 2. Progressive showcase architecture

- [x] 2.1 Extract a compact eager navigation/search index and closed-set section registry that assigns every documented navigation target to the coarse documentation owner; verify structural tests detect missing, duplicate, and unknown owners.
- [x] 2.2 Implement the idempotent section loader with shared in-flight work and accessible idle/loading/ready/error states; verify focused tests cover concurrency, retry, no focus stealing, and single enhancement.
- [x] 2.3 Split the usable eager shell from the coarse deferred documentation graph while preserving rendered ids, fixture identities, markup contracts, and interactions; verify representative component browser tests pass after explicit documentation loading.
- [x] 2.4 Move the complete fidelity manifest, source-reference URL map, and fidelity markup behind the deferred documentation boundary without removing evidence; verify an empty-cache root makes zero generated source-reference requests and the loaded fidelity census matches baseline.
- [x] 2.5 Integrate root load, navigation, search, initial/deferred hashes, typography switching, and hash reveal with the loader; verify keyboard and deep-link browser scenarios across already-loaded and unloaded targets.

## 3. Performance and regression evidence

- [x] 3.1 Generate the candidate-bound performance report and enforce at least 30% lower raw initial JavaScript, zero initial source-reference requests, and no more than 2% initial CSS/font growth against the recorded baseline; verify stale, missing, or reclassified assets fail closed.
- [x] 3.2 Run desktop and narrow-view focused snapshots for shell, loading, error, and representative loaded content plus keyboard, enlarged-text, responsive, request-waterfall, and interaction checks; resolve scope-local regressions and run the Impeccable detector once over changed UI targets.
- [x] 3.3 Build all public packages, run the clean packed-consumer smoke and relevant/full repository tests, and verify `shlz-design-source/`, package exports, component audit counts, and source-derived visuals are unchanged.

## 4. Review and delivery

- [x] 4.1 Run strict OpenSpec validation, post-discovery route conformance, target-diff inspection, and independent Standards/Spec review; resolve every scope-local finding and record any eligible pre-existing limitation with evidence and disposition.
- [x] 4.2 Update the roadmap and durable validation/review evidence with exact before/after bytes, request counts, checks, limitations, CI, and review-thread status; push only the task branch, open an unmerged PR to `main`, and pass the repository delivery guard against the final remote head.
