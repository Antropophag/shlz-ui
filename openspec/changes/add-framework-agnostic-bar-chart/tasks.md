## 1. Evidence and executable contract

- [x] 1.1 Re-attest `Dashboard.svg`, expand the chart source ledger into a component-specific source matrix, inventory every repository occurrence/alternative, and verify the source hash, matrix classifications, and closed occurrence census through source-integrity and occurrence-guard checks.
- [x] 1.2 Add failing pure and browser contract probes for the accepted data, grouped-layout, interaction, legend, table, responsive, and ownership requirements, and verify the unchanged baseline is rejected by the same contract-derived oracles used for the candidate.

## 2. Framework-neutral model and rendering

- [x] 2.1 Implement the normalized rectangular data validator and stable composite datum identity, and verify unit coverage for repeated labels, duplicate IDs, missing pairs, zero values, invalid numbers, empty categories, and the one-to-four-series boundary.
- [x] 2.2 Implement pure visible-series projection, common-scale/grouped geometry, focus-neighbor lookup, and update reconciliation, and verify unit coverage for ordering, hide/restore, last-series protection, scale changes, surviving IDs, and deterministic fallbacks.
- [x] 2.3 Implement source-backed framework-neutral Bar Chart SVG/HTML/CSS rendering inside the existing Chart Widget plot contract, and verify structural plus focused computed-style/snapshot evidence for one through four series, zero values, axes, legend keys, tooltip, and focus-visible states.

## 3. Interaction and accessibility

- [x] 3.1 Implement the web controller for hover/focus tooltip ownership, roving Tab stop, Arrow/Home/End navigation, local focus reveal, update, and teardown, and verify real browser interaction covers every key boundary without unintended page scrolling or leaked listeners.
- [x] 3.2 Implement native legend toggle buttons and the framework-neutral visibility-change notification, and verify pressed states, series ordering, plot/table synchronization, last-visible refusal, focused-series fallback, and notification payloads in browser tests.
- [x] 3.3 Implement the adjacent native table disclosure and semantic visible-data table, and verify header relationships, full display values, expansion persistence, keyboard operation, graphical-rendering failure behavior, and accessibility checks.

## 4. Responsive composition and consumer boundary

- [x] 4.1 Implement the documented minimum plot/group geometry and labelled local horizontal overflow, and verify narrow containers, many categories, one through four series, long/repeated localized labels, long values, local focus scrolling, and absence of page-level overflow.
- [x] 4.2 Add plain-HTML and Showcase fixtures plus one real reporting consumer with consumer-owned filter and period controls, and verify dataset replacement preserves valid IDs without chart-owned fetching, filtering, aggregation, formatting policy, or query-state mutation.

## 5. Documentation, audit, and delivery

- [x] 5.1 Publish the data/interface, supported/unsupported modes, keyboard map, legend/table behavior, responsive rules, filter/period ownership, lifecycle, and adapter boundaries; update the Bar Chart audit manifest and project inventory, and verify every executable root is classified with no unexplained substitute.
- [x] 5.2 Run applicable unit, lint, build, browser, accessibility, focused-visual, consumer, responsive/content-stress, source-integrity, mutation/failure-proof, manual interaction, independent review, conformance, CI, and unmerged-PR delivery gates; record exact counts, limitations, findings, and unresolved threads before making any component-specific completion claim.
