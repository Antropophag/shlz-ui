# Card with action

Presentational promotional composition. Use an `article` with `__content`, optional `__visual`, and `__actions`; place a native `.shlz-button` or `.shlz-link` inside actions. The root is never clickable. The default is the source 314×230 specimen; add `--fluid` for a bounded container down to 240px.
