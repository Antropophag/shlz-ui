# Cover

Static semantic cover composition with eyebrow, heading, description, and metadata. The default preserves the 874×400 source frame; `--fluid` supports bounded layouts down to 320px and grows with content. It owns no navigation or application lifecycle.
