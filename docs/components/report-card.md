# Report card

Presentational report summary with eyebrow, heading, value, metadata, and optional native Link. Use `--muted` for the source `#EEF0F4` variant and `--fluid` for containers down to 240px. Data fetching, loading, empty, and error states remain consumer-owned.
