{
  "kind": "contract",
  "payload": {
    "contractDigest": "c8af1665e38e4ecf361f8a3739f2c1ef26b7286db09a088e0f6270d99057df91",
    "failureInvariants": [],
    "scenarios": [
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer identifies a date column header and its cells as `future`\n- **THEN** the header receives the base-surface bordered source treatment while its body cells retain the base surface and ordinary grid separators",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Consumer marks a future date",
        "name": "Consumer marks a future date",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The temporal header treatment SHALL reproduce the authoritative source: past uses the muted filled time-header treatment, today uses the accent-tinted time-header treatment, and future uses the base surface with its source border. The today treatment SHALL also apply the source subtle fill to every corresponding body cell in the column; past and future body cells SHALL retain the base surface because the authoritative source defines no separate body-cell paint for them. Unavailable cells SHALL retain the source hatch treatment across the cell surface. State text color, emphasis, backgrounds, borders, and separators SHALL use source-mapped repository tokens, and an invented today top rule MUST NOT replace the source treatment.\n\nThe library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer identifies a date column header and its cells as `past`\n- **THEN** the header receives the muted filled source treatment while its body cells retain the base surface and ordinary grid separators",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Consumer marks a past date",
        "name": "Consumer marks a past date",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The temporal header treatment SHALL reproduce the authoritative source: past uses the muted filled time-header treatment, today uses the accent-tinted time-header treatment, and future uses the base surface with its source border. The today treatment SHALL also apply the source subtle fill to every corresponding body cell in the column; past and future body cells SHALL retain the base surface because the authoritative source defines no separate body-cell paint for them. Unavailable cells SHALL retain the source hatch treatment across the cell surface. State text color, emphasis, backgrounds, borders, and separators SHALL use source-mapped repository tokens, and an invented today top rule MUST NOT replace the source treatment.\n\nThe library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer identifies one date column header and its cells as `today`\n- **THEN** the header receives the accent-tinted source treatment and every corresponding body cell receives the subtle source fill while retaining semantic headers and ordinary grid separators",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Consumer marks the current date",
        "name": "Consumer marks the current date",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The temporal header treatment SHALL reproduce the authoritative source: past uses the muted filled time-header treatment, today uses the accent-tinted time-header treatment, and future uses the base surface with its source border. The today treatment SHALL also apply the source subtle fill to every corresponding body cell in the column; past and future body cells SHALL retain the base surface because the authoritative source defines no separate body-cell paint for them. Unavailable cells SHALL retain the source hatch treatment across the cell surface. State text color, emphasis, backgrounds, borders, and separators SHALL use source-mapped repository tokens, and an invented today top rule MUST NOT replace the source treatment.\n\nThe library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer formats dates in its selected locale and timezone\n- **THEN** the grid displays those labels unchanged and performs no independent date parsing or temporal classification",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Locale and timezone stay consumer-owned",
        "name": "Locale and timezone stay consumer-owned",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The temporal header treatment SHALL reproduce the authoritative source: past uses the muted filled time-header treatment, today uses the accent-tinted time-header treatment, and future uses the base surface with its source border. The today treatment SHALL also apply the source subtle fill to every corresponding body cell in the column; past and future body cells SHALL retain the base surface because the authoritative source defines no separate body-cell paint for them. Unavailable cells SHALL retain the source hatch treatment across the cell surface. State text color, emphasis, backgrounds, borders, and separators SHALL use source-mapped repository tokens, and an invented today top rule MUST NOT replace the source treatment.\n\nThe library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** Calendar Grid visual acceptance is evaluated\n- **THEN** focused browser assertions independently compare the computed background, foreground, emphasis, border or separator treatment of past, today, and future headers and body cells against the documented source-backed contract",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Temporal states have independent executable evidence",
        "name": "Temporal states have independent executable evidence",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The temporal header treatment SHALL reproduce the authoritative source: past uses the muted filled time-header treatment, today uses the accent-tinted time-header treatment, and future uses the base surface with its source border. The today treatment SHALL also apply the source subtle fill to every corresponding body cell in the column; past and future body cells SHALL retain the base surface because the authoritative source defines no separate body-cell paint for them. Unavailable cells SHALL retain the source hatch treatment across the cell surface. State text color, emphasis, backgrounds, borders, and separators SHALL use source-mapped repository tokens, and an invented today top rule MUST NOT replace the source treatment.\n\nThe library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer marks a date column unavailable and provides its accessible reason\n- **THEN** the affected header and body cells receive the source-backed unavailable hatch treatment across their surfaces and assistive technology can identify the reason",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Weekend or unavailable date is shown",
        "name": "Weekend or unavailable date is shown",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The temporal header treatment SHALL reproduce the authoritative source: past uses the muted filled time-header treatment, today uses the accent-tinted time-header treatment, and future uses the base surface with its source border. The today treatment SHALL also apply the source subtle fill to every corresponding body cell in the column; past and future body cells SHALL retain the base surface because the authoritative source defines no separate body-cell paint for them. Unavailable cells SHALL retain the source hatch treatment across the cell surface. State text color, emphasis, backgrounds, borders, and separators SHALL use source-mapped repository tokens, and an invented today top rule MUST NOT replace the source treatment.\n\nThe library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      }
    ]
  },
  "version": 1,
  "digest": "062f425a20411f80d1cbdcebbf8960a6b2e9868388913ae1a0ac1dfc145064ae"
}
