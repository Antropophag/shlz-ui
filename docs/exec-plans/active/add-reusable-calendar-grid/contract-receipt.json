{
  "kind": "contract",
  "payload": {
    "contractDigest": "812b523f03e6e1f74577669b4a13efb38141b0fc2871675eb19f1c96896c38fb",
    "failureInvariants": [],
    "scenarios": [
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** source traceability, supported states, semantic relationships, real disclosure, events, focus behavior, two-axis overflow, sticky context, long/empty content, text scaling, plain-HTML use, and a real application consumer pass without a blocking finding\n- **THEN** Calendar Grid may move to `VERIFIED` with exact observed counts, supported limits, and consumer-owned responsibilities recorded",
        "id": "data-display/calendar-grid::Calendar grid audit acceptance::Complete evidence permits verification",
        "name": "Complete evidence permits verification",
        "requirement": "Calendar grid audit acceptance",
        "requirementContent": "Calendar Grid SHALL remain `INVENTORIED` or move to `FINDINGS` until its manifest classifies every repository occurrence and independently passes source integrity, structural contract, runtime browser, accessibility, focused visual, consumer integration, and responsive/content-stress evidence. Static SVGs and forced visual states MUST NOT substitute for real disclosure behavior, keyboard operation, computed sticky/overflow geometry, or consumer-owned rerendering evidence."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** repository or built-DOM census finds calendar-grid markup, an executable fixture, consumer, diagnostic, or legacy substitute absent from the manifest\n- **THEN** the occurrence guard fails and Calendar Grid cannot be marked `VERIFIED`",
        "id": "data-display/calendar-grid::Calendar grid audit acceptance::Unclassified occurrence blocks verification",
        "name": "Unclassified occurrence blocks verification",
        "requirement": "Calendar grid audit acceptance",
        "requirementContent": "Calendar Grid SHALL remain `INVENTORIED` or move to `FINDINGS` until its manifest classifies every repository occurrence and independently passes source integrity, structural contract, runtime browser, accessibility, focused visual, consumer integration, and responsive/content-stress evidence. Static SVGs and forced visual states MUST NOT substitute for real disclosure behavior, keyboard operation, computed sticky/overflow geometry, or consumer-owned rerendering evidence."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer uses the reusable calendar grid\n- **THEN** no employee profile, page title, navigation tabs, statistics view, status filter, or application routing is required by the grid interface",
        "id": "data-display/calendar-grid::Calendar grid semantic structure::Application shell is omitted",
        "name": "Application shell is omitted",
        "requirement": "Calendar grid semantic structure",
        "requirementContent": "The calendar grid SHALL expose its dated matrix as a native table with an accessible name, a row-header column, date column headers, and body cells whose row and column relationships remain available to assistive technology. The public contract SHALL accept consumer-authored labels and content without requiring application profile, tab, filter, statistics, or business-status markup. Visual position alone MUST NOT be the only means of identifying a cell's row or date."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** progressive enhancement is requested for markup missing a required root, row control relationship, or globally unique relationship identifier\n- **THEN** enhancement fails with a descriptive error rather than silently producing an incoherent interaction",
        "id": "data-display/calendar-grid::Calendar grid semantic structure::Invalid enhanced structure fails clearly",
        "name": "Invalid enhanced structure fails clearly",
        "requirement": "Calendar grid semantic structure",
        "requirementContent": "The calendar grid SHALL expose its dated matrix as a native table with an accessible name, a row-header column, date column headers, and body cells whose row and column relationships remain available to assistive technology. The public contract SHALL accept consumer-authored labels and content without requiring application profile, tab, filter, statistics, or business-status markup. Visual position alone MUST NOT be the only means of identifying a cell's row or date."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a server-rendered or plain HTML consumer supplies the documented calendar-grid table structure\n- **THEN** the grid renders without a framework runtime and exposes an accessible table name, row headers, and date column headers",
        "id": "data-display/calendar-grid::Calendar grid semantic structure::Plain HTML consumer renders a grid",
        "name": "Plain HTML consumer renders a grid",
        "requirement": "Calendar grid semantic structure",
        "requirementContent": "The calendar grid SHALL expose its dated matrix as a native table with an accessible name, a row-header column, date column headers, and body cells whose row and column relationships remain available to assistive technology. The public contract SHALL accept consumer-authored labels and content without requiring application profile, tab, filter, statistics, or business-status markup. Visual position alone MUST NOT be the only means of identifying a cell's row or date."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** an item contains a consumer-authored link or button\n- **THEN** native activation and the consumer's handler determine the result while the grid adds no navigation or mutation behavior",
        "id": "data-display/calendar-grid::Consumer ownership and event contract::Consumer handles an item action",
        "name": "Consumer handles an item action",
        "requirement": "Consumer ownership and event contract",
        "requirementContent": "Consumers SHALL own row/date/item identity, ordering, date calculations, domain tones, data loading, errors, permissions, record actions, filtering, selection, persistence, and rerendering. The grid SHALL emit only documented disclosure notifications and MUST NOT expose private DOM geometry or source-screen business vocabulary as interface requirements. Consumer rerenders SHALL be able to preserve disclosure state explicitly rather than depending on undocumented internal state."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer replaces rows or date cells after loading new data\n- **THEN** it can destroy and re-enhance the grid or update documented disclosure state without depending on private implementation details",
        "id": "data-display/calendar-grid::Consumer ownership and event contract::Consumer rerenders data",
        "name": "Consumer rerenders data",
        "requirement": "Consumer ownership and event contract",
        "requirementContent": "Consumers SHALL own row/date/item identity, ordering, date calculations, domain tones, data loading, errors, permissions, record actions, filtering, selection, persistence, and rerendering. The grid SHALL emit only documented disclosure notifications and MUST NOT expose private DOM geometry or source-screen business vocabulary as interface requirements. Consumer rerenders SHALL be able to preserve disclosure state explicitly rather than depending on undocumented internal state."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer identifies one date column as `today`\n- **THEN** the corresponding header and cells receive the documented today treatment while retaining their semantic headers",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Consumer marks the current date",
        "name": "Consumer marks the current date",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer formats dates in its selected locale and timezone\n- **THEN** the grid displays those labels unchanged and performs no independent date parsing or temporal classification",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Locale and timezone stay consumer-owned",
        "name": "Locale and timezone stay consumer-owned",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer marks a date column unavailable and provides its accessible reason\n- **THEN** the column receives the source-backed unavailable treatment and assistive technology can identify that reason",
        "id": "data-display/calendar-grid::Date columns and temporal presentation::Weekend or unavailable date is shown",
        "name": "Weekend or unavailable date is shown",
        "requirement": "Date columns and temporal presentation",
        "requirementContent": "The calendar grid SHALL support consumer-provided date columns with visible primary and secondary labels, optional month or period grouping, and explicit `past`, `today`, `future`, and `unavailable` presentation states. The library SHALL style these states without calculating the current date, locale, week boundary, timezone, work calendar, holiday, or availability. Unavailable columns SHALL remain identifiable without relying only on hatch paint or color."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a user activates a cell overflow control\n- **THEN** the remaining items in that cell become available without changing unrelated rows or cells and focus remains on a connected operable control",
        "id": "data-display/calendar-grid::Progressive row and cell disclosure::Dense cell is disclosed",
        "name": "Dense cell is disclosed",
        "requirement": "Progressive row and cell disclosure",
        "requirementContent": "The optional calendar-grid controller SHALL enhance consumer-authored native buttons for row and dense-cell disclosure. It SHALL synchronize `aria-expanded`, the controlled content's visibility, and emitted bubbling change notifications. Row and cell disclosure MUST be independently controllable, keyboard operable through native button behavior, idempotently enhanceable, and cleanly destroyable. The controller SHALL NOT fetch data, mutate application records, navigate, select dates, choose filters, or infer which rows or cells should begin expanded."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** enhancement is requested twice for the same root and later destroyed\n- **THEN** callers receive the existing live controller, duplicate listeners are not installed, and destruction removes library listeners without deleting consumer markup or data",
        "id": "data-display/calendar-grid::Progressive row and cell disclosure::Enhancement is repeated or destroyed",
        "name": "Enhancement is repeated or destroyed",
        "requirement": "Progressive row and cell disclosure",
        "requirementContent": "The optional calendar-grid controller SHALL enhance consumer-authored native buttons for row and dense-cell disclosure. It SHALL synchronize `aria-expanded`, the controlled content's visibility, and emitted bubbling change notifications. Row and cell disclosure MUST be independently controllable, keyboard operable through native button behavior, idempotently enhanceable, and cleanly destroyable. The controller SHALL NOT fetch data, mutate application records, navigate, select dates, choose filters, or infer which rows or cells should begin expanded."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a user activates a row disclosure button\n- **THEN** only that row's controlled calendar content changes visibility, `aria-expanded` matches the result, and a bubbling grid disclosure event identifies the affected row and state",
        "id": "data-display/calendar-grid::Progressive row and cell disclosure::Row is collapsed and expanded",
        "name": "Row is collapsed and expanded",
        "requirement": "Progressive row and cell disclosure",
        "requirementContent": "The optional calendar-grid controller SHALL enhance consumer-authored native buttons for row and dense-cell disclosure. It SHALL synchronize `aria-expanded`, the controlled content's visibility, and emitted bubbling change notifications. Row and cell disclosure MUST be independently controllable, keyboard operable through native button behavior, idempotently enhanceable, and cleanly destroyable. The controller SHALL NOT fetch data, mutate application records, navigate, select dates, choose filters, or infer which rows or cells should begin expanded."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a cell contains more items than the consumer chooses to show initially\n- **THEN** the visible subset and accessible overflow control communicate the remaining count without losing the cell's row/date context",
        "id": "data-display/calendar-grid::Rows, cells, items, and density states::Dense cell uses an overflow summary",
        "name": "Dense cell uses an overflow summary",
        "requirement": "Rows, cells, items, and density states",
        "requirementContent": "The calendar grid SHALL support consumer-defined row labels, optional row descriptions, empty cells, count-only cells, cells containing one or more generic items, and cells whose item list is visually limited behind an overflow summary. Items SHALL accept consumer text and a documented visual tone without embedding business identifiers or status names in the public contract. Counts and overflow summaries MUST remain consistent with the rendered or consumer-declared item total."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** a consumer places labeled items with supported tones into a cell\n- **THEN** the grid presents the labels in the corresponding date and row without assigning business meaning to the tone",
        "id": "data-display/calendar-grid::Rows, cells, items, and density states::Generic items are rendered",
        "name": "Generic items are rendered",
        "requirement": "Rows, cells, items, and density states",
        "requirementContent": "The calendar grid SHALL support consumer-defined row labels, optional row descriptions, empty cells, count-only cells, cells containing one or more generic items, and cells whose item list is visually limited behind an overflow summary. Items SHALL accept consumer text and a documented visual tone without embedding business identifiers or status names in the public contract. Counts and overflow summaries MUST remain consistent with the rendered or consumer-declared item total."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** row labels, item labels, counts, or cells contain long localized or empty content\n- **THEN** text remains readable, empty state is not conveyed by a misleading count, and controls remain operable without overlapping adjacent cells",
        "id": "data-display/calendar-grid::Rows, cells, items, and density states::Long and empty content remain coherent",
        "name": "Long and empty content remain coherent",
        "requirement": "Rows, cells, items, and density states",
        "requirementContent": "The calendar grid SHALL support consumer-defined row labels, optional row descriptions, empty cells, count-only cells, cells containing one or more generic items, and cells whose item list is visually limited behind an overflow summary. Items SHALL accept consumer text and a documented visual tone without embedding business identifiers or status names in the public contract. Counts and overflow summaries MUST remain consistent with the rendered or consumer-declared item total."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** the available inline size cannot contain all supplied dates\n- **THEN** overflow is contained by the calendar grid, the page does not gain horizontal overflow, and row/date context remains perceivable while the matrix scrolls",
        "id": "data-display/calendar-grid::Two-axis overflow and sticky context::Narrow container scrolls the matrix",
        "name": "Narrow container scrolls the matrix",
        "requirement": "Two-axis overflow and sticky context",
        "requirementContent": "The grid SHALL fit its container, preserve a usable row-header and date-header context while its matrix scrolls, and support both horizontal and vertical overflow without causing horizontal page overflow. Sticky presentation SHALL degrade to ordinary table flow where platform or containment constraints prevent sticking. The public contract SHALL NOT promise virtualization or an unbounded number of simultaneously rendered cells."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** the grid body exceeds its configured viewport height\n- **THEN** vertical scrolling remains inside the grid and the date-header context remains available without covering focusable content",
        "id": "data-display/calendar-grid::Two-axis overflow and sticky context::Tall grid preserves context",
        "name": "Tall grid preserves context",
        "requirement": "Two-axis overflow and sticky context",
        "requirementContent": "The grid SHALL fit its container, preserve a usable row-header and date-header context while its matrix scrolls, and support both horizontal and vertical overflow without causing horizontal page overflow. Sticky presentation SHALL degrade to ordinary table flow where platform or containment constraints prevent sticking. The public contract SHALL NOT promise virtualization or an unbounded number of simultaneously rendered cells."
      },
      {
        "capability": "data-display/calendar-grid",
        "content": "- **WHEN** browser text is enlarged and the grid is used at a narrow supported viewport\n- **THEN** controls and labels remain reachable through ordinary two-axis scrolling with no clipped focus indicator or keyboard trap",
        "id": "data-display/calendar-grid::Two-axis overflow and sticky context::Text scaling does not trap content",
        "name": "Text scaling does not trap content",
        "requirement": "Two-axis overflow and sticky context",
        "requirementContent": "The grid SHALL fit its container, preserve a usable row-header and date-header context while its matrix scrolls, and support both horizontal and vertical overflow without causing horizontal page overflow. Sticky presentation SHALL degrade to ordinary table flow where platform or containment constraints prevent sticking. The public contract SHALL NOT promise virtualization or an unbounded number of simultaneously rendered cells."
      }
    ]
  },
  "version": 1,
  "digest": "446867fe3518112c9bf105617475058f7066f707ed2e798fd9aad17281bf2dd6"
}
