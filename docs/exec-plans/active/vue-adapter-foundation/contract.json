{
  "kind": "contract",
  "payload": {
    "contractDigest": "49c7ba94ce948ebb862c406d5b1c3b94c60ccd0f412f984f9c7235f979c6a698",
    "failureInvariants": [],
    "scenarios": [
      {
        "capability": "adapters/vue-button",
        "content": "- **WHEN** the default Button is activated inside a form\n- **THEN** it does not submit the form\n- **AND** explicit submit/reset buttons retain native form behavior including name/value and external form association",
        "id": "adapters/vue-button::Native Button interface::Form ownership",
        "name": "Form ownership",
        "requirement": "Native Button interface",
        "requirementContent": "`ShlzButton` SHALL render one native button with default slot content, native attributes and native listeners forwarded to that element. Its props SHALL be `variant` (neutral default, primary, text), `size` (md default, sm, xs), `iconOnly` (false default), `type` (button default, submit, reset), and `disabled` (false default). These map to the existing CSS contract with 40/32/26px sizes; icon-only is supported at md/sm only. Consumer class/style and accessible-name attributes SHALL be preserved. The component SHALL expose a typed `element` reference to its native button after mount. Loading, navigation, model state and generated IDs are outside the supported interface."
      },
      {
        "capability": "adapters/vue-button",
        "content": "- **WHEN** an enabled Button is clicked or activated through Enter or Space\n- **THEN** the consumer receives one native click per activation\n- **AND** a disabled Button does not activate and is excluded from sequential keyboard focus",
        "id": "adapters/vue-button::Native Button interface::Native activation and disabled",
        "name": "Native activation and disabled",
        "requirement": "Native Button interface",
        "requirementContent": "`ShlzButton` SHALL render one native button with default slot content, native attributes and native listeners forwarded to that element. Its props SHALL be `variant` (neutral default, primary, text), `size` (md default, sm, xs), `iconOnly` (false default), `type` (button default, submit, reset), and `disabled` (false default). These map to the existing CSS contract with 40/32/26px sizes; icon-only is supported at md/sm only. Consumer class/style and accessible-name attributes SHALL be preserved. The component SHALL expose a typed `element` reference to its native button after mount. Loading, navigation, model state and generated IDs are outside the supported interface."
      },
      {
        "capability": "adapters/vue-button",
        "content": "- **WHEN** a parent changes supported props, attrs or slot content\n- **THEN** the same native button reflects those values and retains the SHLZ base class alongside consumer classes",
        "id": "adapters/vue-button::Native Button interface::Reactive presentation and attributes",
        "name": "Reactive presentation and attributes",
        "requirement": "Native Button interface",
        "requirementContent": "`ShlzButton` SHALL render one native button with default slot content, native attributes and native listeners forwarded to that element. Its props SHALL be `variant` (neutral default, primary, text), `size` (md default, sm, xs), `iconOnly` (false default), `type` (button default, submit, reset), and `disabled` (false default). These map to the existing CSS contract with 40/32/26px sizes; icon-only is supported at md/sm only. Consumer class/style and accessible-name attributes SHALL be preserved. The component SHALL expose a typed `element` reference to its native button after mount. Loading, navigation, model state and generated IDs are outside the supported interface."
      },
      {
        "capability": "adapters/vue-button",
        "content": "- **WHEN** a separate project installs the packed adapter with compatible Vue and styles\n- **THEN** its TypeScript consumer resolves the exported Button and its Node process can import and server-render it without browser globals",
        "id": "adapters/vue-button::Optional package consumption::Isolated consumer",
        "name": "Isolated consumer",
        "requirement": "Optional package consumption",
        "requirementContent": "The private packable `@shlz/vue` package SHALL export typed `ShlzButton` through ESM, support Vue >=3.5.0 <3.6.0 as a peer, and require the consumer to load `@shlz/styles/shlz.css`. Core packages MUST remain usable without Vue. This slice MUST NOT participate in registry publication."
      },
      {
        "capability": "adapters/vue-button",
        "content": "- **WHEN** a server-rendered Button is hydrated with matching props and content\n- **THEN** its original DOM node is retained, no hydration mismatch is reported and activation updates application-owned state once",
        "id": "adapters/vue-button::SSR and hydration::Matching hydration",
        "name": "Matching hydration",
        "requirement": "SSR and hydration",
        "requirementContent": "The adapter SHALL render without browser globals or shared mutable request state. Hydration of matching server/client inputs SHALL preserve server DOM and attach working native interaction without mismatch warnings. Repeated mount/unmount SHALL not duplicate consumer events."
      },
      {
        "capability": "adapters/vue-button",
        "content": "- **WHEN** independent server renders use different labels and a browser consumer remounts a Button repeatedly\n- **THEN** each server result contains only its own content and each browser activation dispatches once to the current consumer",
        "id": "adapters/vue-button::SSR and hydration::Separate renders and remount",
        "name": "Separate renders and remount",
        "requirement": "SSR and hydration",
        "requirementContent": "The adapter SHALL render without browser globals or shared mutable request state. Hydration of matching server/client inputs SHALL preserve server DOM and attach working native interaction without mismatch warnings. Repeated mount/unmount SHALL not duplicate consumer events."
      },
      {
        "capability": "adapters/vue-button",
        "content": "- **WHEN** the supported mode/size matrix and real default, hover, active, focus-visible and disabled states are exercised with icon and long-label content\n- **THEN** computed geometry/paint matches the existing source-backed contract, accessible names and native semantics are preserved, and narrow content remains bounded\n- **AND** a Vue consumer demonstrates application-owned state and lifecycle",
        "id": "adapters/vue-button::Source-backed component evidence::Visual and consumer verification",
        "name": "Visual and consumer verification",
        "requirement": "Source-backed component evidence",
        "requirementContent": "The Vue Button SHALL use the existing source-backed CSS and pass applicable Button completion evidence with independently classified Vue occurrences. Other components MUST NOT inherit its completion status."
      }
    ]
  },
  "version": 1,
  "digest": "19822fbec470ca6573fde839cfe54c129e4bf8deef2d0f052c093b102bb19355"
}
