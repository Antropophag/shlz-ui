{
  "kind": "contract",
  "payload": {
    "contractDigest": "d3ee46b134a86819906f77b02c318c966aa52c5b1a27382c658e47339e25c51f",
    "failureInvariants": [],
    "scenarios": [
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** a consumer marks the upload invalid and provides an identified error message\n- **THEN** the surface exposes the error relationship and error treatment without replacing the consumer's validation text",
        "id": "forms/file-upload::Consumer-owned file composition::Consumer displays an error",
        "name": "Consumer displays an error",
        "requirement": "Consumer-owned file composition",
        "requirementContent": "Selected-file content SHALL remain consumer-authored and MAY compose File Row or Document Row beneath the drop surface. The component MUST NOT infer validation, deduplication, preview, removal, progress, retry, transport, cancellation, persistence, or announcement state."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** a consumer handles selected or dropped files and renders classified file rows\n- **THEN** the enclosing upload composition remains coherent without retaining a library queue or inventing upload lifecycle state",
        "id": "forms/file-upload::Consumer-owned file composition::Consumer renders selected files",
        "name": "Consumer renders selected files",
        "requirement": "Consumer-owned file composition",
        "requirementContent": "Selected-file content SHALL remain consumer-authored and MAY compose File Row or Document Row beneath the drop surface. The component MUST NOT infer validation, deduplication, preview, removal, progress, retry, transport, cancellation, persistence, or announcement state."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** source geometry, native activation, real file drag/drop, events, disabled/error/focus presentation, selected-file composition, narrow layout, enlarged text, plain HTML, and a real application consumer all pass without a blocking finding\n- **THEN** the component may be recorded as `reusable / VERIFIED` with exact occurrence counts and limitations",
        "id": "forms/file-upload::File Upload verification gate::Complete evidence permits verification",
        "name": "Complete evidence permits verification",
        "requirement": "File Upload verification gate",
        "requirementContent": "File Upload SHALL remain below `VERIFIED` until every repository occurrence is classified and source integrity, structural contract, runtime interaction, accessibility, focused visual fidelity, consumer integration, and responsive/content-stress evidence pass independently."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** the native input is disabled\n- **THEN** the complete visible surface communicates its disabled state and neither activation nor dropping emits a library file event",
        "id": "forms/file-upload::Native file selection foundation::Upload is disabled",
        "name": "Upload is disabled",
        "requirement": "Native file selection foundation",
        "requirementContent": "File Upload SHALL use a consumer-authored native file input associated with a visible full-surface label. It SHALL preserve native `accept`, `multiple`, disabled, form, keyboard, and accessible-name behavior without rendering or retaining consumer files."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** a pointer or keyboard user activates the visible drop surface\n- **THEN** the associated native file input receives ordinary platform activation and the consumer receives its native change event after selection",
        "id": "forms/file-upload::Native file selection foundation::User activates the drop surface",
        "name": "User activates the drop surface",
        "requirement": "Native file selection foundation",
        "requirementContent": "File Upload SHALL use a consumer-authored native file input associated with a visible full-surface label. It SHALL preserve native `accept`, `multiple`, disabled, form, keyboard, and accessible-name behavior without rendering or retaining consumer files."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** enhancement is requested repeatedly and later destroyed\n- **THEN** duplicate listeners are not installed and destruction removes library listeners and transient state without deleting consumer markup",
        "id": "forms/file-upload::Optional file-drop enhancement::Enhancement is repeated and destroyed",
        "name": "Enhancement is repeated and destroyed",
        "requirement": "Optional file-drop enhancement",
        "requirementContent": "The optional enhancement SHALL accept files from a real browser drop, expose a real drag-active visual state, and emit one bubbling file event containing the original `FileList`, source (`input` or `drop`), and input element. It MUST reject non-file drags and MUST NOT synthesize an input value, upload files, or replace the consumer's native change handler."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** a user drags files over an enabled enhanced surface and drops them\n- **THEN** drag-active state is cleared and one bubbling file event exposes the dropped `FileList` with source `drop`",
        "id": "forms/file-upload::Optional file-drop enhancement::Files are dropped",
        "name": "Files are dropped",
        "requirement": "Optional file-drop enhancement",
        "requirementContent": "The optional enhancement SHALL accept files from a real browser drop, expose a real drag-active visual state, and emit one bubbling file event containing the original `FileList`, source (`input` or `drop`), and input element. It MUST reject non-file drags and MUST NOT synthesize an input value, upload files, or replace the consumer's native change handler."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** a drag contains no file item\n- **THEN** the surface does not enter file drag-active state or emit a file event",
        "id": "forms/file-upload::Optional file-drop enhancement::Non-file data is dragged",
        "name": "Non-file data is dragged",
        "requirement": "Optional file-drop enhancement",
        "requirementContent": "The optional enhancement SHALL accept files from a real browser drop, expose a real drag-active visual state, and emit one bubbling file event containing the original `FileList`, source (`input` or `drop`), and input element. It MUST reject non-file drags and MUST NOT synthesize an input value, upload files, or replace the consumer's native change handler."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** the containing block is narrower than 467px or text is enlarged to 200 percent\n- **THEN** the surface contracts to the container, the instruction wraps as needed, and activation and focus remain fully visible without horizontal overflow",
        "id": "forms/file-upload::Source-backed visible composition::Available width is narrow",
        "name": "Available width is narrow",
        "requirement": "Source-backed visible composition",
        "requirementContent": "The default File Upload surface SHALL reproduce the `Documents.svg` Upload-Drag composition as one 467×102 dashed surface containing the cloud-upload mark and one centered instruction, with no visually separate action pill. The root SHALL remain fluid up to the source width, and narrow or enlarged-text presentation SHALL reflow without clipped text or focus indication."
      },
      {
        "capability": "forms/file-upload",
        "content": "- **WHEN** File Upload has ordinary available inline space\n- **THEN** its surface is 467px wide by 102px high, its icon and instruction are centered as one composition, and no separate button-shaped action is shown",
        "id": "forms/file-upload::Source-backed visible composition::Default specimen is rendered",
        "name": "Default specimen is rendered",
        "requirement": "Source-backed visible composition",
        "requirementContent": "The default File Upload surface SHALL reproduce the `Documents.svg` Upload-Drag composition as one 467×102 dashed surface containing the cloud-upload mark and one centered instruction, with no visually separate action pill. The root SHALL remain fluid up to the source width, and narrow or enlarged-text presentation SHALL reflow without clipped text or focus indication."
      }
    ]
  },
  "version": 1,
  "digest": "efa83cfe65ca90483969624c76022ef6ce6e05972774e3296920d8a13f943823"
}
