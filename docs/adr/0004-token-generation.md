# ADR 0004: Single-source token generation

Status: accepted.

Author canonical values once in JSON and generate CSS plus distributable JSON. TypeScript imports that generated data. Provenance is a parallel versioned document. This avoids three drifting hand-maintained representations while keeping artifacts easy to consume without JavaScript.
