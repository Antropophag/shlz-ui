# ADR 0001: Figma SVG source authority

Status: accepted.

Original SVG exports are primary evidence. Extraction is a searchable derived index and consumer apps are validation cases. Conflicts resolve in favor of raw SVG. This prevents extraction defects and legacy implementation details from becoming design rules.
