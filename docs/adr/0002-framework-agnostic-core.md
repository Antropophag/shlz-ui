# ADR 0002: Framework-agnostic core

Status: accepted.

Tokens, SVG assets, and scoped CSS have no framework runtime. Vue is an optional future adapter and will not own visual contracts or behavior state machines. This supports plain HTML, PHP/Yii, JavaScript, Vue, and future consumers.
