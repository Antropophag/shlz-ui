# Developer documentation work log — 2026-08-20

This log records decisions and verification evidence, not internal reasoning. The session runs on the isolated local branch `chore/developer-docs-nightly-20260820`; nothing is pushed or merged.

## Iteration 1 — Button and Select pilot

### Scope

- Apply one developer-documentation contract to a CSS-only native Button and a stateful progressively enhanced Select.
- Add executable Showcase guidance and lightweight snippet drift checks.
- Do not change component visual values, public APIs or authoritative source material.

### Changes

- Expanded `docs/components/button.md` and `docs/components/select.md` with purpose, usage/avoid guidance, setup, executable examples, public contracts, states, accessibility, composition, limitations and traceability.
- Added the tested template and review checklist in `docs/component-documentation-template.md` and `docs/component-documentation-review-checklist.md`.
- Added a small shared Showcase renderer for Button and Select developer guidance. Production examples are explicitly distinguished from visual/source diagnostics and unsupported source variants.
- Added metadata/snippet contract tests and a browser assertion for the developer-facing surface.

### Self-review

- Found and corrected an unsupported accessibility implication: the enhanced Select does not currently forward `aria-invalid` or `aria-describedby` from its native value owner to the generated trigger. It is now an explicit limitation and backlog item.
- Found and replaced an irrelevant Button test reference with the actual snippet and browser-test locations.
- Confirmed no changes under `shlz-design-source/`, no component CSS value changes and no public API changes in this iteration.

### Independent review

- Initial result: changes requested — P1 immediate Select teardown in the copyable snippet; P2 inaccurate first Arrow-key behavior; P3 over-broad focus-restoration wording.
- Disposition: all three fixed. Cleanup is now a deferred named lifecycle function, closed/open Arrow behavior is described separately, and focus behavior distinguishes Escape/commit from Tab/outside dismissal. The snippet test rejects the former immediate-destroy sequence.
- Re-review result: approved; no remaining P1/P2/P3.

### Checks

- `node --test tools/tests/component-documentation.test.mjs tools/tests/select-behavior.test.mjs tools/tests/remediation-wave2-source.test.mjs` — 7 passed.
- `npm run build:showcase` — passed.
- Targeted Chromium: base Select, developer usage, keyboard focus — 3 passed; post-review Button/Select subset — 2 passed.
- `npm run lint` — passed.
- `git diff --check` — passed.
- Impeccable detector reported one advisory for the pre-existing grid on `.shlz-visual-fixture`; retained because that surface is explicitly a source-measurement/diagnostic canvas and is outside this iteration.

### Next

- Apply the validated contract to a small related batch of basic form controls, starting with Input and Textarea.

## Iteration 2 — Input and Textarea

### Scope and changes

- Applied the validated component-documentation contract to the native CSS-only Input and Textarea compositions.
- Added executable Showcase guidance and extended the shared snippet checks/browser documentation smoke from two to four components.
- Kept source-only Advanced Input specimens and consumer-owned Textarea counting outside the production API.

### Self-review

- Confirmed native value/label ownership, documented attributes and source counts against `field.css` and `form-controls-source.test.mjs`.
- Kept validation, counter updates and application data outside the style primitive; no public API, visual value or source change was introduced.

### Independent review

- Initial result: P2 — native Textarea resize is clipped by the fixed 58px shell; P3 — Input error message omitted its secondary-row parent; P3 — the named browser coverage was only a layout smoke.
- Disposition: removed misleading `rows`, documented resize as unsupported/clipped pending a separate layout decision, completed the Input error structure, and relabeled browser evidence as layout/navigation smoke.
- Re-review result: approved; no remaining P1/P2/P3.

### Checks

- Component documentation plus form-control source tests — 10 passed after review fixes.
- Targeted Chromium documentation/layout smoke — 2 passed.
- Formatting and `git diff --check` — passed.

### Next

- Migrate a small native choice-control batch, prioritizing Checkbox and Radio before Switch.

## Iteration 3 — Checkbox and Radio

### Scope and changes

- Applied the documentation contract to two CSS-only native choice controls.
- Added copyable labeled Checkbox and grouped Radio examples, exact form/keyboard semantics, source classification and traceability.
- Extended snippet and Showcase documentation checks from four to six components.

### Self-review

- Verified native labeling, same-name Radio grouping, Checkbox indeterminate ownership and source-backed geometry against package CSS and existing browser/source tests.
- Kept selection policy, validation, group composition and application data outside the primitives.

### Independent review

- Initial result: P2 — Checkbox text incorrectly implied boolean form submission; P3 — specific source test missing from traceability; P3 — Radio did not state that fieldset/legend layout is unstyled.
- Disposition: added an explicit `value="yes"` and exact checked/unchecked submission model, linked the source-matrix test separately from the bundle test, and made group reset/layout consumer-owned.
- Re-review result: approved; no remaining P1/P2/P3.

### Checks

- Component documentation, bundle contracts and choice/status source contracts — 19 passed after fixes.
- Targeted Chromium documentation and choice-control geometry/state checks — 2 passed.
- Formatting and `git diff --check` — passed.

### Next

- Document Switch separately, preserving its immediate-setting semantics and reduced-motion contract.

## Iteration 4 — Switch

### Scope and changes

- Applied the documentation contract to the native checkbox-based Switch as a separate semantic batch.
- Added executable HTML and an application-owned native `change` integration example, exact form-data behavior, size/state guidance and reduced-motion traceability.
- Extended snippet and Showcase checks from six to seven components.

### Self-review

- Verified `role="switch"`, stable labeling, native state ownership, string form submission, 38×20/24×14 geometry and reduced-motion behavior against package CSS/tests.
- Kept persistence, optimistic updates, rollback, loading and error announcements outside the primitive and did not imply that the illustrative listener implements them.

### Independent review

- Result: approved on first review; no P1/P2/P3.
- Reviewer independently confirmed semantics, form data, sizes/states, motion, source traceability and the consumer-owned persistence boundary.

### Checks

- Component documentation and choice/status source tests — 10 passed.
- Targeted Chromium documentation and choice geometry/state checks — 2 passed.
- Formatting and `git diff --check` — passed.

### Next

- Move to a small display-component batch: Status and Badge, keeping consumer-owned business semantics explicit.

## Iteration 5 — Status and Badge

### Scope and changes

- Added the first full documentation pages for Status and Badge and exposed them in Showcase developer guidance.
- Documented Status as text-owned business meaning over source paint families and Badge as a contextual, non-interactive count/dot.
- Extended snippet and Showcase checks from seven to nine components.

### Self-review

- Verified paint modifiers, geometry and distinct source families against production CSS, generated source tests and browser geometry checks.
- Avoided inventing success/warning/error mappings, count caps or live-region behavior. Added accessible count context and explicit decorative/meaningful dot rules.

### Independent review

- Result: approved on first review; no P1/P2/P3.
- Reviewer confirmed text-owned semantics, color-independent guidance, visually-hidden count context, dot/live-update boundaries, variants and traceability.

### Checks

- Component documentation and choice/status source tests — 11 passed.
- Targeted Chromium documentation and Status/Badge geometry checks — 2 passed.
- Formatting and `git diff --check` — passed.

### Next

- Evaluate Tag and Person Tag as the next related data-label batch; keep removable behavior and identity data consumer-owned unless an existing contract proves otherwise.

## Iteration 6 — Tag and Person Tag

### Scope and changes

- Applied the documentation contract to two distinct source families sharing the 30px tag shell.
- Added non-interactive generic Tag guidance, Person Tag avatar/identity guidance, closable markup and a standalone removal integration.
- Extended snippet checks to all examples and Showcase mapping from nine to eleven component docs.

### Self-review

- Verified separate source sets, source-backed geometry, icon exports, decorative repeated avatar, native remove-button naming and consumer ownership.
- Kept generic Tag non-interactive and left identity lookup, avatar privacy/loading, persistence and post-removal focus policy outside the primitive.

### Independent review

- Initial result: P2 — closable copyable markup exposed a dead focusable button without an activation hook.
- Disposition: added an application-owned hook and operational standalone click handler; documented that stateful apps must update source state instead of mutating DOM; extended snippet tests to require this contract.
- Re-review result: approved; no remaining P1/P2/P3.

### Checks

- Component documentation, Tag source and shared component contract tests — 19 passed before review; focused post-review suite — 9 passed.
- Targeted Chromium Showcase documentation check — passed.
- Formatting and `git diff --check` — passed.

### Next

- Document Segment as the next native selection composition, then prefer final suite/report work over widening into complex overlays unless time remains.

## Iteration 7 — Segment

### Scope and changes

- Applied the documentation contract to the native radio-based segmented value selector.
- Added copyable fieldset/radio markup, source-backed sizes, explicit non-navigation boundary, accessibility/keyboard contract and responsive limitations.
- Extended snippet and Showcase checks from eleven to twelve component docs.

### Self-review

- Verified the required input/adjacent-label DOM, same-name unique-value grouping, three source sizes and absent selected+disabled source combination.
- Did not invent tabs, toggle-button behavior, equal widths, wrapping or responsive overflow.

### Independent review

- Initial result: P2 — visible legend was overclaimed inside the fixed shell; P3 — test did not enforce the adjacent sibling selector.
- Disposition: restricted the shell to `legend.shlz-visually-hidden`, made any visible question surrounding consumer content, and asserted exactly one checked radio plus two direct input→label sibling sequences.
- Re-review result: approved; no remaining P1/P2/P3.

### Checks

- Component documentation, Segment source and shared component contracts — 20 passed before review; focused post-review suite — 10 passed.
- Targeted Chromium Showcase documentation and source-critical geometry — 2 passed.
- Formatting and `git diff --check` — passed.

### Next

- Run the broader repository quality gates, consolidate the final backlog/report and only begin another component batch if the remaining window safely permits a complete review cycle.

## Iteration 8 — Link and Avatar

### Scope and changes

- Applied the documentation contract to native navigation and the visual identity primitive.
- Added real-href Link and named text/image Avatar snippets, usage boundaries, accessibility guidance and source/code/test traceability.
- Rendered both developer sections beside their existing complete source matrices and extended snippet/browser coverage from twelve to fourteen components.

### Self-review

- Verified Link states and Avatar's four sizes × three content types against production CSS and source tests.
- Kept routing, interactive avatar composition, image loading/privacy and failed-image fallback consumer-owned; no visual values or public API changed.

### Independent review

- Initial result: P2 — `aria-label` on a generic text Avatar was unreliable; P3 — roleless unavailable Link incorrectly used `aria-disabled`.
- Disposition: made the standalone text Avatar a named `role=img`, documented the preferred decorative-with-visible-name mode, and standardized unavailable Link as non-interactive `.shlz-link--disabled` text without ARIA.
- Recheck found the old ARIA pattern still present in the Showcase specimen/tests; removed it and updated both contracts.
- Final re-review: approved; no remaining P1/P2/P3.

### Checks

- Component documentation and Wave 3 source tests — 13 passed.
- Targeted Chromium documentation and native-semantics checks — 2 passed.
- Formatting and `git diff --check` — passed.

### Next

- Run broad quality gates and consolidate the remaining documentation backlog before selecting another batch.

## Iteration 9 — Tabs

### Scope and changes

- Applied the documentation contract to the stateful Tabs behavior as a single-component batch.
- Added complete tab↔panel markup, explicit behavior initialization/teardown, automatic-activation keyboard guidance, visual-family boundaries and lifecycle limitations.
- Added the developer section to the existing Tabs Showcase article and extended snippet/browser coverage to fifteen component docs.

### Self-review

- Traced initialization, activation, disabled handling, roving tabindex, panel scoping and destroy semantics directly through `tabs.ts`.
- Corrected click wording to preserve native button focus behavior and limited disabled-skipping claims to the implemented keyboard path.

### Independent review

- Initial result: P2 — plain-text panels lacked a keyboard stop; P3 — snippet tests fully paired only the second tab and panel.
- Disposition: added conditional `tabindex=0` to both plain-text panels, documented when to omit it, and asserted both bidirectional ARIA pairs plus initial visible/hidden and selected/tabindex state.
- Re-review result: approved; no remaining P1/P2/P3.

### Checks

- Component documentation, Tabs source and shared behavior contracts — 22 passed.
- Targeted Chromium automatic-activation and documentation checks — 2 passed.
- Formatting and `git diff --check` — passed.

### Next

- Run the broad repository gate; then use its evidence to decide whether another small component batch is safe.

## Iteration 10 — documentation/visual baseline separation

### Scope and changes

- Marked every rendered developer-doc panel as an additive Showcase surface.
- Kept component-specific visual snapshots focused on production specimens and taught the aggregate `#components` capture to hide only developer docs while retaining its other intentional additions.
- Did not update or accept any visual snapshot.

### Self-review

- Confirmed normal Showcase rendering and the dedicated docs browser test still expose every panel.
- Confirmed existing visual harness behavior already defines the additive-surface boundary, so no new snapshot abstraction was introduced.

### Independent review

- Initial result: P2 — the aggregate primitives snapshot bypassed the shared harness and still captured docs.
- Disposition: added test-only `.shlz-developer-docs` isolation to that capture rather than using the broad harness, which would also remove other intentional additive UI.
- Re-review result: approved; no remaining P1/P2/P3.

### Checks

- Component documentation tests — 11 passed.
- Complete Chromium `components-next` suite — 9 passed with existing snapshots unchanged.
- Aggregate primitives baseline plus docs visibility — 2 passed with existing snapshot unchanged.
- `git diff --check` — passed.

### Next

- Re-run the broad browser gate; investigate only reproducible residual failures and do not normalize transient snapshot noise.
